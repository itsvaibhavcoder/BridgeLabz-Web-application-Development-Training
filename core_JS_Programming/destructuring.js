//Destructuring Assignment:
//A syntax that allows unpacking values from arrays or properties from objects.

let [a,b] = [1,2];
console.log(a,b);

let {name,age} = {name: "vaibhav", age: 25};
console.log(name, age); 